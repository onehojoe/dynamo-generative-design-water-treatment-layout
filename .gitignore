# ── 비공개 (절대 커밋 금지) ───────────────────────────────
_private/
_book/
*원고*
*교재*
*발주*
*견적*

# ── Revit ────────────────────────────────────────────────
*.0001.rvt
*.0002.rvt
*.0003.rvt
*.[0-9][0-9][0-9][0-9].rvt
*.[0-9][0-9][0-9][0-9].rfa
*.slog
*.dwl
*.dwl2
*.rws
Backup/

# ── Dynamo ───────────────────────────────────────────────
*.dyn.bak
*.backup
DynamoRevit*.log
.dynamo/

# ── Python ───────────────────────────────────────────────
__pycache__/
*.pyc
.venv/

# ── Windows ──────────────────────────────────────────────
Thumbs.db
desktop.ini
~$*

# ── 대용량 주의 ───────────────────────────────────────────
# GitHub 단일 파일 상한 100MB. 큰 샘플은 Releases 로 올린다.
# 의도적으로 넣을 때만:  git add -f <파일>
*.nwd
*.nwc

# local distribution zips
_dist/
